## Customize the test machine
machine:
  services:
    - docker

test:
  override:
    - ./bin/build-test-snapshot.sh


deployment:
  docker-hub:
    branch: master
    commands:
      - ./bin/publish-snapshot-containers.sh
